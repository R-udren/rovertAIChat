# Database Configuration
DATABASE_PASSWORD=your_secure_password

# Domain Configuration (set to your domain for production, localhost for development)
DOMAIN=localhost

# Frontend Configuration 
# For development, include both localhost and 127.0.0.1 with common ports
# For production, use your actual domain
FRONTEND_ORIGINS=http://localhost:3000,http://localhost:80,http://127.0.0.1:3000,http://127.0.0.1:80,https://yourdomain.com
VITE_API_BASE_URL=http://localhost:8000  # For development, https://yourdomain.com for production

# Environment (development or production)
ENVIRONMENT=development

# JWT Configuration - Generate secure keys for production!
JWT_SECRET_KEY=dev_secret_key_change_in_production
JWT_REFRESH_SECRET_KEY=dev_refresh_secret_key_change_in_production

# PgAdmin Configuration (optional - uncomment to enable)
# PGADMIN_DEFAULT_EMAIL=admin@example.com
# PGADMIN_DEFAULT_PASSWORD=your_admin_password
